import React from 'react';
import { DashboardView } from '../../src/components/dashboard/DashboardView';

export default function DashboardPage() {
  return <DashboardView onNavigate={() => {}} onNewBook={() => {}} />;
}
